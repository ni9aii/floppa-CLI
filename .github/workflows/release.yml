name: Release floppa-cli

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  linux:
    name: Linux (${{ matrix.target }})
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            label: x86_64
          - runner: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
            label: aarch64
    steps:
      - uses: actions/checkout@v6

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential pkg-config libssl-dev binutils

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: "floppa-cli -> floppa-cli/target"

      - name: Build floppa-cli
        run: cargo build --release --manifest-path floppa-cli/Cargo.toml --target ${{ matrix.target }}

      - name: Strip binary
        run: |
          BIN="floppa-cli/target/${{ matrix.target }}/release/floppa-cli"
          # Use the cross strip for aarch64; plain strip for the native x86_64 build.
          if [ "${{ matrix.label }}" = "aarch64" ]; then
            aarch64-linux-gnu-strip "$BIN" || true
          else
            strip "$BIN" || true
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: floppa-cli-linux-${{ matrix.label }}
          path: floppa-cli/target/${{ matrix.target }}/release/floppa-cli

  release:
    name: Create GitHub Release
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [linux]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Download x86_64 artifact
        uses: actions/download-artifact@v4
        with:
          name: floppa-cli-linux-x86_64
          path: release-artifacts

      - name: Download aarch64 artifact
        uses: actions/download-artifact@v4
        with:
          name: floppa-cli-linux-aarch64
          path: release-artifacts

      - name: List artifacts
        run: find release-artifacts -type f | sort

      - name: Create release
        uses: softprops/action-gh-release@v3
        with:
          draft: true
          generate_release_notes: true
          files: |
            release-artifacts/floppa-cli-linux-x86_64
            release-artifacts/floppa-cli-linux-aarch64
